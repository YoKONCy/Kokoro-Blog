# 国际化（i18n）

> 当前：纯中文。架构预留多语言扩展。

## 使用

```typescript
import { t } from '@/lib/i18n';
t('nav.home') // → "首页"
```

## 扩展策略

1. `locales/` 添加语言文件（`en.ts`、`ja.ts`）
2. 扩展 `Locale` 类型
3. 添加语言切换 UI
4. 考虑 URL 前缀路由（`/en/blog/...`）
